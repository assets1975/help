—пециальные параметры, те параметры которые уже установлены и доступны дл€ пользовател€. Ёти параметры невозможно назначить внутри скрипта. —писок таких параметров:

$1 - $9 - Ёто так называемые позиционные параметры, служат дл€ передачи аргументов командной строки. 
          ≈сли аргументов больше 9, то они должны заключатьс€ в фигурные скобки{} 
$0 - »м€ скрипта который был запущен. 
$# -  оличество аргументов командной строки или позиционных параметров
$? -  од с которым была завершена предыдуща€ команда. ≈сли команда была выполнена удачно, 
     то значение этой переменной будет 0, если же неудачно то не 0. 
$$ - Ќомер процесса под которым исполн€етс€ данный скрипт, очень удобно использовать 
     в имени файла что бы сделать его уникальным. 
$! - Ќомер последнего, запущенного в фоне процесса. 
$  -   —писок флагов переданных сценарию. √одитьс€ разве что дл€ проверки интерактивного режима.
$* - ƒанный параметр содержит в себе все параметры переданные из командной строки. 
$@ - “о же самое что $*, только каждый параметр представлен отдельно.
$_ - —пециальна€ переменна€, содержит последний аргумент предыдущей команды.
---------------------------------------------------------------------------------------------------------------