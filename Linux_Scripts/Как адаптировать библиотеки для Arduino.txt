#Как адаптировать библиотеки для Arduino 1.0
#нужно просто открыть файлы библиотеки (.h и .cpp) и если в них встречается строчка

#include "WProgram.h"
то заменить её на: 
#if defined(ARDUINO) && ARDUINO >= 100
  #include "Arduino.h"
#else
  #include "WProgram.h"
#endif 